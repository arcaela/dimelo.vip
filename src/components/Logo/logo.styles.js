import { makeStyles } from '@material-ui/core/styles';

export const logoStyles = makeStyles((theme) => ({
    main:{
     padding: '1rem'
    },
    img:{
        maxWidth: '100%',
        height: 'auto',
        width: '120px'
    }
}))